apiVersion: v1
kind: Pod
metadata:
  name: test-scheduler-pod
  namespace: default
  labels:
    app: test-scheduler
spec:
  schedulerName: custom-scheduler
  containers:
  - name: alpine
    image: 192.168.200.184:5000/leoyy6/alpine:3.19
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      limits:
        memory: "128Mi"
        cpu: "500m"
  restartPolicy: Never
